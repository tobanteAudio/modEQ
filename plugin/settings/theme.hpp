#pragma once

namespace tobanteAudio
{
static const auto ORANGE     = juce::Colour(255, 87, 34);
static const auto WHITESMOKE = juce::Colour(238, 238, 238);
static const auto BLUE       = juce::Colour(45, 64, 89);
static const auto DARKGRAY   = juce::Colour(34, 40, 49);

}  // namespace tobanteAudio
